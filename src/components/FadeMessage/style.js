import styled from 'styled-components';

export const Container = styled.View`
	position: absolute;
	justify-content: flex-end;
	align-items: center;
	top: 0;
	right: 0;
	bottom: 0;
	left: 0;
`;
