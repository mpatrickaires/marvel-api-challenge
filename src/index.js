import React from 'react';
import { SafeAreaView, Text } from 'react-native';

const App = props => {
    return (
        <SafeAreaView>
            <Text></Text>
        </SafeAreaView>
    );
};

export default App;
